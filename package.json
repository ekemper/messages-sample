{
  "name": "simple-todos",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --driver-package=dispatch:mocha-browser",
    "test-app-watch": "meteor test --full-app --driver-package dispatch:mocha-browser",
    "chimp-watch": "chimp --ddp=http://localhost:3000 --watch --mocha --path=tests",
    "chimp-test": "chimp --mocha --path=tests"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "bcrypt": "^1.0.2",
    "bootstrap": "^4.0.0-alpha.5",
    "denodeify": "^1.2.1",
    "meteor-node-stubs": "~0.2.4",
    "path": "^0.12.7",
    "tether": "^1.4.0"
  },
  "devDependencies": {
    "faker": "^4.1.0"
  }
}
